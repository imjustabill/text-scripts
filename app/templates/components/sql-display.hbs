{{#if sqlList}}
  <h4 class="margin-top padding-top">SQL in scripts</h4>

  <textarea id="sqlCode" class="pre-textarea" readonly="readonly">{{#each sqlList as |data|}}{{data}}
{{/each}}
  </textarea>
{{/if}}